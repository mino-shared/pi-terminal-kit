# Minimal core for tmux mouse handling.
mouse-reporting = true
mouse-shift-capture = never

# Plain clicks are reported to tmux. Hold Shift and drag, then press Cmd+C, to
# select and copy terminal text. Use Cmd+Shift+click to open detected links.
# Ctrl+S needs no synthetic Ghostty mapping because tmux passes it through.
