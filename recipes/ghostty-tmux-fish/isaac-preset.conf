# Optional opinionated preset. Review before copying.
# Ghostty
font-size = 14
window-padding-x = 8
window-padding-y = 8

# tmux lines, copy into tmux.conf rather than Ghostty config:
# set -g status-position top
# set -g status-interval 2
# bind | split-window -h -c '#{pane_current_path}'
# bind - split-window -v -c '#{pane_current_path}'
